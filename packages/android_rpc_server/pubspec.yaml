name: android_rpc_server
description: Android RPC Server.
version: 0.0.1

publish_to: "none"

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mime: ^1.0.1
  riverpod: ^1.0.3
  shelf: ^1.2.0
  shelf_router: ^1.1.2
  shelf_static: ^1.1.0
  sms_repository:
    path: ../sms_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - static/
